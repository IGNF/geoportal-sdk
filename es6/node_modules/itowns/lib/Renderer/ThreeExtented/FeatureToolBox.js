'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _getIterator2 = require('babel-runtime/core-js/get-iterator');

var _getIterator3 = _interopRequireDefault(_getIterator2);

var _three = require('three');

var THREE = _interopRequireWildcard(_three);

var _earcut = require('earcut');

var _earcut2 = _interopRequireDefault(_earcut);

var _Coordinates = require('../../Core/Geographic/Coordinates');

var _Ellipsoid = require('../../Core/Math/Ellipsoid');

var _Ellipsoid2 = _interopRequireDefault(_Ellipsoid);

var _BasicMaterial = require('../../Renderer/BasicMaterial');

var _BasicMaterial2 = _interopRequireDefault(_BasicMaterial);

var _Lines = require('../../Renderer/Lines');

var _Lines2 = _interopRequireDefault(_Lines);

var _NodeMesh = require('../../Renderer/NodeMesh');

var _NodeMesh2 = _interopRequireDefault(_NodeMesh);

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function prepareToRtc(object3D, center) {
    object3D.matrixWorld.elements = new Float64Array(16);
    object3D.matrix.elements = new Float64Array(16);
    object3D.position.copy(center);
    object3D.updateMatrixWorld();
    object3D.matrixAutoUpdate = false;
    object3D.matrixWorldNeedsUpdate = false;
} /**
   * Generated On: 2016-09-28
   * Class: FeatureToolBox
   * Description:
   */

function FeatureToolBox() {
    this.ellipsoid = new _Ellipsoid2.default((0, _Coordinates.ellipsoidSizes)());
}

var geoCoord = new _Coordinates.C.EPSG_4326(0, 0, 0);

geoCoord.set = function set(longitude, latitude, altitude) {
    geoCoord._values[0] = longitude;
    geoCoord._values[1] = latitude;
    geoCoord._values[2] = altitude;
};

FeatureToolBox.prototype.geoArrayTo3D = function geoArrayTo3D(point, altitude, pointOrder) {
    return this.geoCoordTo3D(parseFloat(point[pointOrder.long]), parseFloat(point[pointOrder.lat]), altitude);
};

FeatureToolBox.prototype.geoCoordTo3D = function geoCoordTo3D(longitude, latitude, altitude) {
    geoCoord.set(longitude, latitude, altitude);
    return geoCoord.as('EPSG:4978').xyz();
};

FeatureToolBox.prototype.GeoJSON2Polygon = function GeoJSON2Polygon(features, pointOrder) {
    var polyGroup = new THREE.Object3D();
    for (var r = 0; r < features.length; r++) {
        var positions = [];
        var polygon = features[r].geometry.coordinates[0][0];
        var altitude = features[r].properties.z_min;
        if (polygon.length > 2 && altitude != 9999) {
            for (var j = 0; j < polygon.length; ++j) {
                var spt = this.geoArrayTo3D(polygon[j], altitude, pointOrder);
                positions.push(spt.x, spt.y, spt.z);
            }
            var geometry = new THREE.BufferGeometry();
            var material = new THREE.LineBasicMaterial({ color: 0xff0000, transparent: true, opacity: 0.9 });
            geometry.addAttribute('position', new THREE.BufferAttribute(new Float32Array(positions), 3));
            geometry.computeBoundingSphere();
            var poly = new THREE.Line(geometry, material);
            poly.frustumCulled = false;
            polyGroup.add(poly);
        }
    }
    return polyGroup;
};

FeatureToolBox.prototype.GeoJSON2Box = function GeoJSON2Box(features, pointOrder) {
    pointOrder = pointOrder || { long: 0, lat: 1 };
    var bboxGroup = new THREE.Object3D();
    var wallGeometry = new THREE.Geometry(); // for the walls
    var roofGeometry = new THREE.Geometry(); // for the roof

    // FIXME : see with geojson standard
    // to get coordinates
    var typeCoords = !isNaN(features[0].geometry.coordinates[0][0][0][0]);
    var cc = typeCoords ? features[0].geometry.coordinates[0][0][0] : features[0].geometry.coordinates[0][0];
    var center = this.geoArrayTo3D(cc, 0, pointOrder);

    var _iteratorNormalCompletion = true;
    var _didIteratorError = false;
    var _iteratorError = undefined;

    try {
        for (var _iterator = (0, _getIterator3.default)(features), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
            var feature = _step.value;

            var height = feature.height || feature.properties.hauteur;
            var altitude = 0;
            if (feature.properties) {
                altitude = feature.properties.z_min - height;
            }

            var arrPoint2D = [];
            var polygon = typeCoords ? feature.geometry.coordinates[0][0] : feature.geometry.coordinates[0];

            var white = void 0;
            var black = void 0;

            if (!isNaN(feature.id)) {
                white = new THREE.Color(0xffffff);
                black = new THREE.Color(0x000000);
            } else {
                var type = feature.id.substr(0, feature.id.indexOf('.'));
                if (type === 'bati_indifferencie') {
                    white = new THREE.Color(0xffffff);
                    black = new THREE.Color(0x000000);
                } else if (type === 'bati_remarquable') {
                    white = new THREE.Color(0x5555ff);
                    black = new THREE.Color(0x000055);
                } else if (type === 'bati_industriel') {
                    white = new THREE.Color(0xff5555);
                    black = new THREE.Color(0x550000);
                }
            }

            if (polygon.length > 2) {
                // VERTICES
                for (var j = 0; j < polygon.length - 1; ++j) {
                    var pgeo2 = this.geoArrayTo3D(polygon[j], altitude + height, pointOrder).sub(center);
                    var pgeo1 = this.geoArrayTo3D(polygon[j], altitude, pointOrder).sub(center);

                    arrPoint2D.push(parseFloat(polygon[j][pointOrder.long]), parseFloat(polygon[j][pointOrder.lat]));
                    wallGeometry.vertices.push(pgeo1, pgeo2);
                }
                // FACES
                // indice of the first point of the polygon 3D
                for (var k = wallGeometry.vertices.length - (polygon.length - 1) * 2; k < wallGeometry.vertices.length; k += 2) {
                    var l = k; // % (pts2DTab.length);
                    if (l > wallGeometry.vertices.length - 4) {
                        l = wallGeometry.vertices.length - (polygon.length - 1) * 2;
                    }

                    var faceA = new THREE.Face3(l + 3, l + 1, l);
                    var faceB = new THREE.Face3(l + 2, l + 3, l);

                    faceA.vertexColors = [white, white, black];
                    faceB.vertexColors = [black, white, black];

                    wallGeometry.faces.push(faceA);
                    wallGeometry.faces.push(faceB);
                }

                var ll = wallGeometry.vertices.length - (polygon.length - 1) * 2;

                var faceC = new THREE.Face3(ll, ll + 1, wallGeometry.vertices.length - 1);
                var faceD = new THREE.Face3(ll, wallGeometry.vertices.length - 1, wallGeometry.vertices.length - 2);

                faceC.vertexColors = [black, white, white];
                faceD.vertexColors = [black, white, black];

                wallGeometry.faces.push(faceC);
                wallGeometry.faces.push(faceD);
            }

            wallGeometry.computeFaceNormals(); // WARNING : VERY IMPORTANT WHILE WORKING WITH RAY CASTING ON CUSTOM MESH

            //* *************** ROOF ****************************
            // TODO: Use wall building to build roof, and remove cartographicToCartesian using
            var triangles = (0, _earcut2.default)(arrPoint2D);
            for (var i = 0; i < triangles.length; i += 3) {
                roofGeometry.vertices.push(this.geoCoordTo3D(arrPoint2D[triangles[i] * 2], arrPoint2D[triangles[i] * 2 + 1], altitude + height).sub(center));
                roofGeometry.vertices.push(this.geoCoordTo3D(arrPoint2D[triangles[i + 1] * 2], arrPoint2D[triangles[i + 1] * 2 + 1], altitude + height).sub(center));
                roofGeometry.vertices.push(this.geoCoordTo3D(arrPoint2D[triangles[i + 2] * 2], arrPoint2D[triangles[i + 2] * 2 + 1], altitude + height).sub(center));

                var face = new THREE.Face3(roofGeometry.vertices.length - 3, roofGeometry.vertices.length - 2, roofGeometry.vertices.length - 1);

                face.vertexColors = [white, white, white];

                roofGeometry.faces.push(face);
            }
        }
    } catch (err) {
        _didIteratorError = true;
        _iteratorError = err;
    } finally {
        try {
            if (!_iteratorNormalCompletion && _iterator.return) {
                _iterator.return();
            }
        } finally {
            if (_didIteratorError) {
                throw _iteratorError;
            }
        }
    }

    roofGeometry.computeFaceNormals();

    var wallMat = new _BasicMaterial2.default(new THREE.Color(features.style.color || '#C6BCB0'), features.style.opacity || 1.0);

    var roofMat = new _BasicMaterial2.default(new THREE.Color(features.style.colorRoof || '#839498'), features.style.opacity || 1.0);

    wallMat.uniforms.lightingEnabled.value = true;
    roofMat.uniforms.lightingEnabled.value = false;

    wallMat.uniforms.enabledCutColor.value = false;
    roofMat.uniforms.enabledCutColor.value = false;

    wallMat.transparent = true;
    roofMat.transparent = true;

    var wall = new THREE.Mesh(wallGeometry, wallMat);
    wall.frustumCulled = false;
    var roof = new THREE.Mesh(roofGeometry, roofMat);
    roof.frustumCulled = false;

    bboxGroup.add(wall);
    wall.add(roof);

    prepareToRtc(bboxGroup, center);

    return bboxGroup;
};

/**
 * Process the data received from a WFS request with a tile of feature type 'Line'.
 * Can be used whe the type of the feature tile is a Grid and not a quadTree
 * @param features: the data received as JSON inside a tab
 * @param box:      the tile bounding box (rad)
 * @param layer:    the current layer with specific parameters
 */
FeatureToolBox.prototype.GeoJSON2Line = function GeoJSON2Line(features, box, layer, pointOrder) {
    var lines = new THREE.Object3D();
    var center = new THREE.Vector3();
    var altitude = layer.params.altitude;

    for (var i = 0; i < features.length; i++) {
        var feature = features[i];
        var coords = feature.geometry.coordinates;

        if (coords.length) {
            if (i === 0) {
                center.copy(this.geoArrayTo3D(coords[0], altitude, pointOrder));
            }

            var colorLine = new THREE.Color();

            this.manageColor(feature.properties, colorLine, layer);

            var line = new _Lines2.default({
                linewidth: 20.0,
                useTexture: false,
                opacity: 1.0,
                sizeAttenuation: true,
                color: colorLine
            });

            for (var k = 0; k < coords.length; k++) {
                var pt = this.geoArrayTo3D(coords[k], altitude, pointOrder).sub(center);
                line.addPoint(pt);
            }

            line.process();

            lines.add(line);
        }
    }

    prepareToRtc(lines, center);

    return lines;
};

/**
 * Create the entire geometry of the object passed in. Is use to create feature geometry
 * like points or boxes.
 * @param features: the data received as JSON inside a tab
 * @param bbox:     the tile bounding box (rad)
 * @param type:     the type of mesh for this layer
 * @param layer:    the current layer with specific parameters
 * @param node:     the current node
 */
FeatureToolBox.prototype.GeoJSON2Point = function GeoJSON2Point(features, bbox, layer, pointOrder) {
    var points = new THREE.Object3D();
    var center = new THREE.Vector3();
    points.geometry = new THREE.Geometry();
    points.material = new _BasicMaterial2.default();
    points.material.uniforms.lightingEnabled.value = false;

    for (var i = 0; i < features.length; i++) {
        var point = new _NodeMesh2.default();
        var feature = features[i];
        var coords = feature.geometry.coordinates;
        var _geoCoord = new _Coordinates.C.EPSG_4326(coords[pointOrder.long], coords[pointOrder.lat], (bbox.bottom() + bbox.top()) / 2 + 3);
        var normalGlobe = this.ellipsoid.geodeticSurfaceNormalCartographic(_geoCoord);
        var centerPoint = this.ellipsoid.cartographicToCartesian(_geoCoord);

        if (i === 0) {
            center.copy(centerPoint);
        }

        centerPoint.sub(center);

        var params = layer.params;
        if (layer.type == 'box') {
            point.geometry = new THREE.BoxGeometry(params.boxWidth || 40, params.boxWidth || 40, params.boxHeight || 80);
        } else if (layer.type == 'point') {
            point.geometry = new THREE.CircleGeometry(params.radius || 10, params.nbSegment || 3, params.thetaStart || 0, params.thetaLength || 2 * Math.PI);
        } else {
            continue;
        }

        // TODO: replace by point.lookAt(normalGlobe);
        point.geometry.lookAt(normalGlobe);
        point.position.copy(centerPoint);

        var colorPoint = new THREE.Color();
        this.manageColor(feature.properties, colorPoint, layer);

        point.material = new _BasicMaterial2.default(colorPoint);

        points.add(point);
    }

    prepareToRtc(points, center);

    return points;
};

/**
 * Manage to put the colors inside the color manager for a feature type 'Point'.
 * @param properties: properties of the feature
 * @param color:      manager of the color of a face
 * @params layer:     the current layer with specific parameters
 */
FeatureToolBox.prototype.manageColor = function manageColor(properties, color, layer) {
    var colorParams = layer.params.color || undefined;

    if (colorParams !== undefined) {
        for (var i = 0; i < colorParams.testTab.length; i++) {
            if (properties[colorParams.property] === colorParams.testTab[i]) {
                color.setHex(colorParams.colorTab[i]);
                return;
            }
        }
    }
    color.setHex(new THREE.Color(0xFFFFFF));
};

FeatureToolBox.prototype.createGeometryArray = function createGeometryArray(json) {
    var geometry_array = [];

    if (json.type == 'Feature') {
        geometry_array.push(json.geometry);
    } else if (json.type == 'FeatureCollection') {
        for (var feature_num = 0; feature_num < json.features.length; feature_num++) {
            geometry_array.push(json.features[feature_num].geometry);
        }
    } else if (json.type == 'GeometryCollection') {
        for (var geom_num = 0; geom_num < json.geometries.length; geom_num++) {
            geometry_array.push(json.geometries[geom_num]);
        }
    } else {
        throw new Error('The geoJSON is not valid.');
    }
    // alert(geometry_array.length);
    return geometry_array;
};

FeatureToolBox.prototype.needsInterpolation = function needsInterpolation(point2, point1) {
    // If the distance between two latitude and longitude values is
    // greater than five degrees, return true.
    var lon1 = point1[0];
    var lat1 = point1[1];
    var lon2 = point2[0];
    var lat2 = point2[1];
    var lon_distance = Math.abs(lon1 - lon2);
    var lat_distance = Math.abs(lat1 - lat2);

    return lon_distance > 5 || lat_distance > 5;
};

FeatureToolBox.prototype.interpolatePoints = function interpolatePoints(interpolation_array) {
    // This function is recursive. It will continue to add midpoints to the
    // interpolation array until needsInterpolation() returns false.
    var temp_array = [];

    for (var point_num = 0; point_num < interpolation_array.length - 1; point_num++) {
        var point1 = interpolation_array[point_num];
        var point2 = interpolation_array[point_num + 1];

        if (this.needsInterpolation(point2, point1)) {
            temp_array.push(point1);
            var midpoint_lon = (point1[0] + point2[0]) / 2;
            var midpoint_lat = (point1[1] + point2[1]) / 2;
            var midpoint = [midpoint_lon, midpoint_lat];
            temp_array.push(midpoint);
        } else {
            temp_array.push(point1);
        }
    }

    temp_array.push(interpolation_array[interpolation_array.length - 1]);

    if (temp_array.length > interpolation_array.length) {
        return this.interpolatePoints(temp_array);
    } else {
        return temp_array;
    }
};

FeatureToolBox.prototype.createCoordinateArray = function createCoordinateArray(feature) {
    // Loop through the coordinates and figure out if the points need interpolation.
    var temp_array = [];

    for (var point_num = 0; point_num < feature.length; point_num++) {
        var point1 = feature[point_num];
        var point2 = feature[point_num - 1];

        if (point_num > 0) {
            if (this.needsInterpolation(point2, point1)) {
                var interpolation_array = [point2, point1];
                interpolation_array = this.interpolatePoints(interpolation_array);

                for (var inter_point_num = 0; inter_point_num < interpolation_array.length; inter_point_num++) {
                    temp_array.push(interpolation_array[inter_point_num]);
                }
            } else {
                temp_array.push(point1);
            }
        } else {
            temp_array.push(point1);
        }
    }
    return temp_array;
};

FeatureToolBox.prototype.intersectsegment = function intersectsegment(a, b, i, p) {
    var d = new THREE.Vector2();
    var e = new THREE.Vector2();
    d.x = b.x - a.x;
    d.y = b.y - a.y;
    e.x = p.x - i.x;
    e.y = p.y - i.y;
    var denom = d.x * e.y - d.y * e.x;
    if (denom == 0.0) {
        return -1;
    } // erreur, cas limite
    var t = -(a.x * e.y - i.x * e.y - e.x * a.y + e.x * i.y) / denom;
    if (t < 0.0 || t >= 1.0) {
        return 0;
    }
    var u = -(-d.x * a.y + d.x * i.y + d.y * a.x - d.y * i.x) / denom;
    if (u < 0.0 || u >= 1.0) {
        return 0;
    }
    return 1;
};

/*
 * Function that tests if a point p is inside a polygon using the classic
 * Ray Casting algorithm
 * @param {type} posGeo
 * @returns {undefined}
 */
FeatureToolBox.prototype.inPolygon = function inPolygon(p, arrPoints) {
    var k = new THREE.Vector2(6.0, 75.0); // This point should be out of the polygon
    var nbintersections = 0;
    for (var i = 0; i < arrPoints.length - 1; i++) {
        var a = arrPoints[i];
        var b = arrPoints[i + 1]; // .xy;
        var iseg = this.intersectsegment(a, b, k, p);
        nbintersections += iseg;
    }
    return nbintersections % 2 === 1;
};

/**
 *
 * display feature attribute information at position p (lonlat)
 */
FeatureToolBox.prototype.showFeatureAttributesAtPos = function showFeatureAttributesAtPos(p, polygons) {
    var intersect = false;
    var properties;
    var i = 0;
    polygons = polygons || this.arrPolygons;
    while (!intersect && i < polygons.length) {
        intersect = this.inPolygon(p, polygons[i].polygon);
        i++;
    }

    i--;
    if (intersect && polygons[i].properties.description !== undefined) {
        properties = polygons[i].properties;
    }

    if (!intersect) return undefined;

    return properties;
};

FeatureToolBox.prototype.drawLine = function drawLine(coordOrigin, tileWH, p1, p2, thickness, ctx, prop) {
    var tilePx = 256;
    ctx.strokeStyle = prop.stroke; // "rgba(255, 0, 255, 0.5)";//"rgba(1,1,0,1)";
    ctx.lineWidth = prop['stroke-width'];
    ctx.globalAlpha = prop['stroke-opacity'];
    ctx.beginPath();
    var a = p1.sub(coordOrigin);
    var b = p2.sub(coordOrigin);
    a.divideScalar(tileWH.x).multiplyScalar(tilePx);
    b.divideScalar(tileWH.x).multiplyScalar(tilePx);
    //      console.log("aa",coordOrigin,tileWH,p1,p2,a,b);
    //       if( (a.x>=0 && a.x<= tilePx && a.y >=0 && a.y <=tilePx)
    //          && (b.x>=0 && b.x<= tilePx && b.y >=0 && b.y <=tilePx)){
    ctx.moveTo(a.x, tilePx - a.y);
    ctx.lineTo(b.x, tilePx - b.y);
    ctx.stroke();
    ctx.globalAlpha = 1.0;
    //      }
    // }
};

/*
 * Draw 2D polygon in tile (rasterized)
 * @param {type} polygon
 * @param {type} coordOrigin
 * @param {type} tileWH
 * @param {type} ctx
 * @param {type} prop
 * @returns {undefined}
 */
FeatureToolBox.prototype.drawPolygon = function drawPolygon(polygon, coordOrigin, tileWH, ctx, prop) {
    var tilePx = 256;
    ctx.strokeStyle = prop.stroke;
    ctx.lineWidth = prop['stroke-width'];
    ctx.fillStyle = prop.fill; // "rgba(255, 0, 255, 0.5)";//"rgba(1,1,0,1)";
    ctx.globalAlpha = prop['fill-opacity'];
    // ctx["stroke-opacity"] = 0.8;
    // console.log(ctx.strokeStyle);
    ctx.beginPath();

    for (var i = 0; i < polygon.length - 1; ++i) {
        var p1 = polygon[i];
        var p2 = polygon[i + 1];
        var a = p1.clone().sub(coordOrigin);
        var b = p2.clone().sub(coordOrigin);

        a.divideScalar(tileWH.x).multiplyScalar(tilePx);
        b.divideScalar(tileWH.x).multiplyScalar(tilePx);

        if (i === 0) {
            ctx.moveTo(a.x, tilePx - a.y);
            ctx.lineTo(b.x, tilePx - b.y);
        } else {
            ctx.lineTo(b.x, tilePx - b.y);
        }
    }
    ctx.closePath();
    ctx.globalAlpha = prop['fill-opacity'];
    ctx.fill();
    ctx.globalAlpha = prop['stroke-opacity'];
    ctx.stroke();
    ctx.globalAlpha = 1.0;
};

// parameters in deg, vec2
FeatureToolBox.prototype.createRasterImage = function createRasterImage(bbox, features) {
    var origin = new THREE.Vector2(bbox.west(_Coordinates.UNIT.DEGREE), bbox.south(_Coordinates.UNIT.DEGREE));
    var dimension = bbox.dimensions(_Coordinates.UNIT.RADIAN);
    var size = new THREE.Vector2(dimension.x, dimension.y).divideScalar(Math.PI / 180);

    var c = document.createElement('canvas');
    c.width = 256;
    c.height = 256;
    var ctx = c.getContext('2d');
    // Lines
    for (var j = 0; j < features.lines.length; ++j) {
        var line = features.lines[j].line;
        var properties = features.lines[j].properties;
        for (var i = 0; i < line.length - 1; ++i) {
            this.drawLine(origin, size, line[i].clone(), line[i + 1].clone(), 4, ctx, properties);
        }
    }
    // Polygon
    for (var _i = 0; _i < features.polygons.length; ++_i) {
        this.drawPolygon(features.polygons[_i].polygon, origin, size, ctx, features.polygons[_i].properties);
    }

    var texture = new THREE.Texture(c);
    texture.flipY = true; // FALSE by default on THREE.DataTexture but True by default for THREE.Texture!
    texture.needsUpdate = true;
    texture.name = 'featureRaster';
    return texture;
};

// Extract polygons and lines for raster rendering in GPU
FeatureToolBox.prototype.extractFeatures = function extractFeatures(json) {
    var arrPolygons = [];
    var arrLines = [];

    for (var nFeature = 0; nFeature < json.features.length; nFeature++) {
        var feat = json.features[nFeature];
        if (feat.geometry.type === 'LineString') {
            var arrLine = [];
            for (var point_num = 0; point_num < feat.geometry.coordinates.length; point_num++) {
                var v = feat.geometry.coordinates[point_num];
                arrLine.push(new THREE.Vector2(v[0], v[1]));
            }
            arrLines.push({ line: arrLine, properties: feat.properties });
        }

        if (feat.geometry.type === 'Polygon') {
            var arrPolygon = [];
            for (var _point_num = 0; _point_num < feat.geometry.coordinates.length; _point_num++) {
                for (var p = 0; p < feat.geometry.coordinates[_point_num].length; ++p) {
                    var _v = feat.geometry.coordinates[_point_num][p];
                    arrPolygon.push(new THREE.Vector2(_v[0], _v[1]));
                }
            }
            arrPolygons.push({ polygon: arrPolygon, properties: feat.properties });
        }
    }
    this.arrPolygons = arrPolygons;
    this.arrLines = arrLines;
    return { lines: arrLines, polygons: arrPolygons };
};

FeatureToolBox.prototype.createFeaturesPoints = function createFeaturesPoints(json) {
    var globalObject = new THREE.Object3D();
    var pointOrder = { long: 0, lat: 1 };

    for (var nFeature = 0; nFeature < json.features.length; nFeature++) {
        var feat = json.features[nFeature];
        if (feat.geometry.type === 'Point') {
            var vertex = this.geoArrayTo3D(feat.geometry.coordinates, feat.geometry.coordinates[2], pointOrder);
            if (feat.properties.name != null) {
                globalObject.add(this.createText(vertex, feat.properties));
            } else {
                globalObject.add(this.createIcon(vertex));
            }
        }
    }
    return globalObject;
};

FeatureToolBox.prototype.processingGeoJSON = function processingGeoJSON(json) {
    // TODO: Why this function?
    // There are GeoJSON2Line and GeoJSON2Point

    var jsonFeatures = this.createGeometryArray(json);
    var geometry = new THREE.Geometry();
    var pointOrder = { long: 0, lat: 1 };
    var bpoint = false;
    var coordinate_array = [];

    var jsonFeaturesToVertex = function jsonFeaturesToVertex(coordinates, alti, geom) {
        coordinate_array = this.createCoordinateArray(coordinates);
        var _iteratorNormalCompletion2 = true;
        var _didIteratorError2 = false;
        var _iteratorError2 = undefined;

        try {
            for (var _iterator2 = (0, _getIterator3.default)(coordinate_array), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
                var coordinate = _step2.value;

                var v = this.geoArrayTo3D(coordinate, alti, pointOrder);
                if (geom) {
                    geom.vertices.push(v);
                } else {
                    geometry.vertices.push(v);
                }
            }
        } catch (err) {
            _didIteratorError2 = true;
            _iteratorError2 = err;
        } finally {
            try {
                if (!_iteratorNormalCompletion2 && _iterator2.return) {
                    _iterator2.return();
                }
            } finally {
                if (_didIteratorError2) {
                    throw _iteratorError2;
                }
            }
        }
    }.bind(this);

    var center = void 0;
    var poly = new THREE.Object3D();

    var _iteratorNormalCompletion3 = true;
    var _didIteratorError3 = false;
    var _iteratorError3 = undefined;

    try {
        for (var _iterator3 = (0, _getIterator3.default)(jsonFeatures), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
            var feature = _step3.value;

            var altitude = feature.altitude || json.altitude;
            if (feature.type == 'Point') {
                var vertex = this.geoArrayTo3D(feature.coordinates, altitude, pointOrder);
                geometry.vertices.push(vertex);
                bpoint = true;
            } else if (feature.type == 'MultiPoint') {
                for (var point_num = 0; point_num < feature.coordinates.length; point_num++) {
                    coordinate_array = feature.coordinates[point_num];
                    var _vertex = this.geoArrayTo3D(coordinate_array, altitude, pointOrder);
                    geometry.vertices.push(_vertex);
                }
                bpoint = true;
            } else if (feature.type == 'LineString') {
                jsonFeaturesToVertex(feature.coordinates, altitude);
            } else if (feature.type == 'Polygon') {
                var _iteratorNormalCompletion4 = true;
                var _didIteratorError4 = false;
                var _iteratorError4 = undefined;

                try {
                    for (var _iterator4 = (0, _getIterator3.default)(feature.coordinates), _step4; !(_iteratorNormalCompletion4 = (_step4 = _iterator4.next()).done); _iteratorNormalCompletion4 = true) {
                        var coords = _step4.value;

                        var colorLine = new THREE.Color(json.stroke || 0xff0000);
                        var _line = new _Lines2.default({
                            linewidth: json.strokeWidth || 1.0,
                            useTexture: false,
                            opacity: json.strokeOpacity || 1.0,
                            sizeAttenuation: false,
                            color: colorLine
                        });

                        var _iteratorNormalCompletion5 = true;
                        var _didIteratorError5 = false;
                        var _iteratorError5 = undefined;

                        try {
                            for (var _iterator5 = (0, _getIterator3.default)(coords), _step5; !(_iteratorNormalCompletion5 = (_step5 = _iterator5.next()).done); _iteratorNormalCompletion5 = true) {
                                var coord = _step5.value;

                                if (!center) {
                                    center = this.geoArrayTo3D(coord, altitude, pointOrder);
                                }
                                var pt = this.geoArrayTo3D(coord, altitude, pointOrder);
                                pt.sub(center);
                                _line.addPoint(pt);
                            }
                        } catch (err) {
                            _didIteratorError5 = true;
                            _iteratorError5 = err;
                        } finally {
                            try {
                                if (!_iteratorNormalCompletion5 && _iterator5.return) {
                                    _iterator5.return();
                                }
                            } finally {
                                if (_didIteratorError5) {
                                    throw _iteratorError5;
                                }
                            }
                        }

                        _line.process();
                        poly.add(_line);
                    }
                } catch (err) {
                    _didIteratorError4 = true;
                    _iteratorError4 = err;
                } finally {
                    try {
                        if (!_iteratorNormalCompletion4 && _iterator4.return) {
                            _iterator4.return();
                        }
                    } finally {
                        if (_didIteratorError4) {
                            throw _iteratorError4;
                        }
                    }
                }
            } else if (feature.type == 'MultiLineString') {
                for (var segment_num = 0; segment_num < feature.coordinates.length; segment_num++) {
                    jsonFeaturesToVertex(feature.coordinates, altitude);
                }
            } else if (feature.type == 'MultiPolygon') {
                var _geometry = new THREE.Geometry();
                var bboxGroup = new THREE.Object3D();
                var normal = new THREE.Vector3();
                var _center = new THREE.Vector3();
                var cc = true;
                var minALtitude = 100000;
                var _iteratorNormalCompletion6 = true;
                var _didIteratorError6 = false;
                var _iteratorError6 = undefined;

                try {
                    for (var _iterator6 = (0, _getIterator3.default)(feature.coordinates), _step6; !(_iteratorNormalCompletion6 = (_step6 = _iterator6.next()).done); _iteratorNormalCompletion6 = true) {
                        var polygons = _step6.value;
                        var _iteratorNormalCompletion7 = true;
                        var _didIteratorError7 = false;
                        var _iteratorError7 = undefined;

                        try {
                            for (var _iterator7 = (0, _getIterator3.default)(polygons), _step7; !(_iteratorNormalCompletion7 = (_step7 = _iterator7.next()).done); _iteratorNormalCompletion7 = true) {
                                var polygon = _step7.value;

                                var vertexs = [];
                                var _iteratorNormalCompletion8 = true;
                                var _didIteratorError8 = false;
                                var _iteratorError8 = undefined;

                                try {
                                    for (var _iterator8 = (0, _getIterator3.default)(polygon), _step8; !(_iteratorNormalCompletion8 = (_step8 = _iterator8.next()).done); _iteratorNormalCompletion8 = true) {
                                        var _vertex2 = _step8.value;

                                        minALtitude = Math.min(minALtitude, _vertex2[2]);
                                        var cartesien = this.geoCoordTo3D(_vertex2[0], _vertex2[1], _vertex2[2]);
                                        if (cc) {
                                            cc = false;
                                            _center.copy(cartesien);
                                            normal.copy(_center).normalize();
                                            bboxGroup.quaternion.setFromUnitVectors(new THREE.Vector3(0, 1, 0), normal);
                                            prepareToRtc(bboxGroup, _center);
                                        }
                                        bboxGroup.worldToLocal(cartesien);
                                        vertexs = vertexs.concat(cartesien.toArray());
                                    }
                                } catch (err) {
                                    _didIteratorError8 = true;
                                    _iteratorError8 = err;
                                } finally {
                                    try {
                                        if (!_iteratorNormalCompletion8 && _iterator8.return) {
                                            _iterator8.return();
                                        }
                                    } finally {
                                        if (_didIteratorError8) {
                                            throw _iteratorError8;
                                        }
                                    }
                                }

                                var triangles = (0, _earcut2.default)(vertexs, null, 3);
                                for (var i = 0; i < triangles.length; i += 3) {
                                    var v1 = new THREE.Vector3(vertexs[triangles[i] * 3], vertexs[triangles[i] * 3 + 1], vertexs[triangles[i] * 3 + 2]);
                                    var v2 = new THREE.Vector3(vertexs[triangles[i + 1] * 3], vertexs[triangles[i + 1] * 3 + 1], vertexs[triangles[i + 1] * 3 + 2]);
                                    var v3 = new THREE.Vector3(vertexs[triangles[i + 2] * 3], vertexs[triangles[i + 2] * 3 + 1], vertexs[triangles[i + 2] * 3 + 2]);
                                    _geometry.vertices.push(v1);
                                    _geometry.vertices.push(v2);
                                    _geometry.vertices.push(v3);

                                    var face = new THREE.Face3(_geometry.vertices.length - 3, _geometry.vertices.length - 2, _geometry.vertices.length - 1);

                                    _geometry.faces.push(face);
                                }
                            }
                        } catch (err) {
                            _didIteratorError7 = true;
                            _iteratorError7 = err;
                        } finally {
                            try {
                                if (!_iteratorNormalCompletion7 && _iterator7.return) {
                                    _iterator7.return();
                                }
                            } finally {
                                if (_didIteratorError7) {
                                    throw _iteratorError7;
                                }
                            }
                        }
                    }
                } catch (err) {
                    _didIteratorError6 = true;
                    _iteratorError6 = err;
                } finally {
                    try {
                        if (!_iteratorNormalCompletion6 && _iterator6.return) {
                            _iterator6.return();
                        }
                    } finally {
                        if (_didIteratorError6) {
                            throw _iteratorError6;
                        }
                    }
                }

                _geometry.computeFaceNormals();

                var wallMat = new _BasicMaterial2.default(new THREE.Color(json.stroke), 1.0);

                wallMat.side = THREE.DoubleSide;
                wallMat.uniforms.lightOn.value = true;
                wallMat.uniforms.enabledCutColor.value = true;
                wallMat.transparent = true;

                var wall = new THREE.Mesh(_geometry, wallMat);
                wall.frustumCulled = false;

                bboxGroup.add(wall);

                minALtitude -= json.translate || 0.0;

                bboxGroup.translateOnAxis(new THREE.Vector3(0, 1, 0), -minALtitude);
                wall.translateOnAxis(new THREE.Vector3(0, 1, 0), -minALtitude);
                bboxGroup.updateMatrixWorld(true);

                return bboxGroup;
            } else {
                throw new Error('The geoJSON is not valid.');
            }
        }
    } catch (err) {
        _didIteratorError3 = true;
        _iteratorError3 = err;
    } finally {
        try {
            if (!_iteratorNormalCompletion3 && _iterator3.return) {
                _iterator3.return();
            }
        } finally {
            if (_didIteratorError3) {
                throw _iteratorError3;
            }
        }
    }

    if (poly.children.length) {
        prepareToRtc(poly, center);
        return poly;
    }

    if (!bpoint) {
        var material = new THREE.LineBasicMaterial({ color: 0xff0000, linewidth: 1 });
        var line = new THREE.Line(geometry, material);
        return line;
    } else {
        var _material = new THREE.PointsMaterial({ color: 0xff0000, size: 100 });
        return new THREE.Points(geometry, _material);
    }
};

/*
 * Create icon at position for point geometry
 * Icon size should be constant. -> need a specific shader
 * @returns {FeatureToolBox.prototype.createIcon.texture|THREE.Texture}
 */
FeatureToolBox.prototype.createIcon = function createIcon(pos) {
    var image = document.createElement('img');
    var texture = new THREE.Texture(image);
    image.onload = function onload() {
        texture.needsUpdate = true;
    };
    image.src = 'data:image/png;base64,';
    var materialS = new THREE.SpriteMaterial({ map: texture, color: 0xffffff, depthTest: false });
    var sprite = new THREE.Sprite(materialS);
    sprite.scale.set(500, 500, 500);
    sprite.position.copy(pos);

    var material = new THREE.LineBasicMaterial({ color: 0xffffff, transparent: false }); // side:THREE.DoubleSide, , linewidth: 5
    var geometry = new THREE.Geometry();
    geometry.vertices.push(new THREE.Vector3(), new THREE.Vector3(-pos.x, -pos.y, -pos.z).divideScalar(1000));
    var line = new THREE.Line(geometry, material);
    sprite.add(line);
    return sprite;
};

FeatureToolBox.prototype.createText = function createText(pos, prop) {
    var sprite = makeTextSprite(prop.name);
    sprite.position.copy(pos);

    var material = new THREE.LineBasicMaterial({ color: 0xffffff, transparent: false }); // side:THREE.DoubleSide, , linewidth: 5,
    var geometry = new THREE.Geometry();
    geometry.vertices.push(new THREE.Vector3(), new THREE.Vector3(-pos.x, -pos.y, -pos.z).divideScalar(1000));
    var line = new THREE.Line(geometry, material);
    sprite.add(line);

    return sprite;
};

function makeTextSprite(message, parameters) {
    if (parameters === undefined) parameters = {};
    var fontface = {}.propertyIsEnumerable.call(parameters, 'fontface') ? parameters.fontface : 'Arial';

    var fontsize = {}.propertyIsEnumerable.call(parameters, 'fontsize') ? parameters.fontsize : 22;

    var borderThickness = {}.propertyIsEnumerable.call(parameters, 'borderThickness') ? parameters.borderThickness : 4;

    var borderColor = {}.propertyIsEnumerable.call(parameters, 'borderColor') ? parameters.borderColor : { r: 0, g: 0, b: 0, a: 1.0 };

    var backgroundColor = {}.propertyIsEnumerable.call(parameters, 'backgroundColor') ? parameters.backgroundColor : { r: 255, g: 255, b: 255, a: 0.8 };

    // var spriteAlignment = THREE.SpriteAlignment.topLeft;

    var canvas = document.createElement('canvas');
    canvas.width = 256;
    canvas.height = 128;
    var context = canvas.getContext('2d');
    context.font = 'Bold ' + fontsize + 'px ' + fontface;

    // get size data (height depends only on font size)
    var metrics = context.measureText(message);
    var textWidth = metrics.width;

    // background color
    context.fillStyle = 'rgba(' + backgroundColor.r + ',' + backgroundColor.g + ',' + backgroundColor.b + ',' + backgroundColor.a + ')';
    // border color
    context.strokeStyle = 'rgba(' + borderColor.r + ',' + borderColor.g + ',' + borderColor.b + ',' + borderColor.a + ')';
    context.lineWidth = borderThickness;
    roundRect(context, borderThickness / 2, borderThickness / 2, textWidth + borderThickness, fontsize * 1.4 + borderThickness, 6);
    // 1.4 is extra height factor for text below baseline: g,j,p,q.
    // text color
    context.fillStyle = 'rgba(0, 0, 0, 1.0)';
    context.fillText(message, borderThickness, fontsize + borderThickness);
    // canvas contents will be used for a texture
    var texture = new THREE.Texture(canvas);
    texture.needsUpdate = true;

    var spriteMaterial = new THREE.SpriteMaterial({ map: texture, color: 0xffffff, depthTest: false }); // , useScreenCoordinates: false} );
    var sprite = new THREE.Sprite(spriteMaterial);
    sprite.scale.set(1000, 1000, 1000); // 100,50,1.0);
    return sprite;
}

// function for drawing rounded rectangles
function roundRect(ctx, x, y, w, h, r) {
    ctx.beginPath();
    ctx.moveTo(x + r, y);
    ctx.lineTo(x + w - r, y);
    ctx.quadraticCurveTo(x + w, y, x + w, y + r);
    ctx.lineTo(x + w, y + h - r);
    ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h);
    ctx.lineTo(x + r, y + h);
    ctx.quadraticCurveTo(x, y + h, x, y + h - r);
    ctx.lineTo(x, y + r);
    ctx.quadraticCurveTo(x, y, x + r, y);
    ctx.closePath();
    ctx.fill();
    ctx.stroke();
}

exports.default = FeatureToolBox;