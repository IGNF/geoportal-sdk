/* global describe, it */

define(["chai", "vg"], function (chai, VirtualGeo) {

    var assert = chai.assert;
    var expect = chai.expect;
    var should = chai.should();

    describe("-- Test VirtualGeo --", function () {
        describe("VirtualGeo Interface", function () {

             var VG = null ;
/*
             beforeEach( function (done) {
                 require(["virtual/VG"], function (_VG) {
                     VG = _VG;
                     done();
                 });
             });

            it("VirtualGeo exists", function () {
            expect(VG).to.exist ;
        });*/

        });
    });
});
